# RustMQ Development Environment Variables
# Copy this file to .env and update with actual values
#
# SECURITY WARNING: NEVER commit .env to git!
# The .env file should be added to .gitignore

# =============================================================================
# MinIO Configuration (S3-compatible storage)
# =============================================================================
# Generate secure credentials with: openssl rand -base64 32

MINIO_ACCESS_KEY=changeme-generate-with-openssl-rand
MINIO_SECRET_KEY=changeme-generate-with-openssl-rand

# =============================================================================
# RustMQ Security (REQUIRED)
# =============================================================================
# Certificate private key encryption password
# CRITICAL: This is MANDATORY - RustMQ will fail to start without it
#
# Generate with: openssl rand -base64 32
# Minimum 16 characters, recommended 32+

RUSTMQ_KEY_ENCRYPTION_PASSWORD=changeme-generate-with-openssl-rand

# =============================================================================
# Google Cloud (for BigQuery subscriber - optional)
# =============================================================================
# Only required if using the BigQuery subscriber component

GCP_PROJECT_ID=your-gcp-project-id
BIGQUERY_DATASET=your-dataset
BIGQUERY_TABLE=your-table
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# =============================================================================
# Quick Setup Instructions
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate secure values:
#    export MINIO_ACCESS_KEY="$(openssl rand -base64 32)"
#    export MINIO_SECRET_KEY="$(openssl rand -base64 32)"
#    export RUSTMQ_KEY_ENCRYPTION_PASSWORD="$(openssl rand -base64 32)"
#
# 3. Update .env file with the generated values above
#
# 4. Start services:
#    docker-compose up -d
#
# =============================================================================
# Security Best Practices
# =============================================================================
#
# ✓ Use minimum 32 characters for all secrets
# ✓ Generate with cryptographically secure random (openssl rand)
# ✓ Store production secrets in secret manager (Google Secret Manager, AWS Secrets Manager, Vault)
# ✓ Never commit .env to git (already in .gitignore)
# ✓ Rotate credentials annually
# ✓ Document recovery procedure for RUSTMQ_KEY_ENCRYPTION_PASSWORD
#
